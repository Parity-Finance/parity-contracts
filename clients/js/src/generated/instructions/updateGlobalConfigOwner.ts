/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type UpdateGlobalConfigOwnerInstructionAccounts = {
  globalConfig: PublicKey | Pda;
  newOwner: Signer;
};

// Data.
export type UpdateGlobalConfigOwnerInstructionData = {
  discriminator: Array<number>;
};

export type UpdateGlobalConfigOwnerInstructionDataArgs = {};

export function getUpdateGlobalConfigOwnerInstructionDataSerializer(): Serializer<
  UpdateGlobalConfigOwnerInstructionDataArgs,
  UpdateGlobalConfigOwnerInstructionData
> {
  return mapSerializer<
    UpdateGlobalConfigOwnerInstructionDataArgs,
    any,
    UpdateGlobalConfigOwnerInstructionData
  >(
    struct<UpdateGlobalConfigOwnerInstructionData>(
      [['discriminator', array(u8(), { size: 8 })]],
      { description: 'UpdateGlobalConfigOwnerInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [119, 181, 100, 47, 35, 150, 97, 109],
    })
  ) as Serializer<
    UpdateGlobalConfigOwnerInstructionDataArgs,
    UpdateGlobalConfigOwnerInstructionData
  >;
}

// Instruction.
export function updateGlobalConfigOwner(
  context: Pick<Context, 'programs'>,
  input: UpdateGlobalConfigOwnerInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'ptStaking',
    '6cxnuwSaJgaBsq6szLNGQ3UMibUB7XNv1mpoC91t37yv'
  );

  // Accounts.
  const resolvedAccounts = {
    globalConfig: {
      index: 0,
      isWritable: true as boolean,
      value: input.globalConfig ?? null,
    },
    newOwner: {
      index: 1,
      isWritable: true as boolean,
      value: input.newOwner ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdateGlobalConfigOwnerInstructionDataSerializer().serialize(
    {}
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
